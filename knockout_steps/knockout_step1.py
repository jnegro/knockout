#!/usr/bin/python

# STEP 1 - Bootstrap the program
# During this step, we set up the first parts of the code that the program needs to
# to function properly.

# This line is to make sure that our program is compatible with different versions
# of Python that might be installed on different computers
from __future__ import print_function


def main():
    # Our main function will go here.  For now, we print a test:
    print("HELLO BOXERS!")


if __name__ == "__main__":

    # run the main function
    main()
